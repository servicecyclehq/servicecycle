# ServiceCycle Python extraction dependencies
# Pin versions to prevent silent regressions on Docker rebuild.
# To update: test with new version, update this file, rebuild Docker image.
#
# Runtime image is node:20-slim (Debian) as of commit c652578 — all packages
# resolve against manylinux/glibc wheels. If a new version drops wheel support
# for the runtime target, the Docker build will fail loudly rather than
# silently compiling from source.

pdfplumber==0.11.4
pypdf==4.3.1
pytesseract==0.3.13
# [2026-07-05 CVE bump] Was >=10.4.0,<12.0 -- that ceiling blocked the fixed
# versions for CVE-2026-25990 (OOB write, fixed 12.1.1), CVE-2026-40192 and
# CVE-2026-42311 (decompression-bomb DoS, fixed 12.2.0) from ever being
# picked up, even though docs/security/DEPENDENCY_DECISIONS.md's accepted-risk
# entries assumed a future bump would close them. Verified against the golden-
# set extraction eval + jest extractor suites at 12.2.0 with zero regressions
# (see docs/security/DEPENDENCY_DECISIONS.md for the verification note) --
# bumping instead of continuing to accept the risk.
Pillow>=12.2.0,<13.0
# pypdfium2 is not pinned explicitly — pdfplumber pulls it as a transitive dep
# and Debian ships the manylinux wheel, so the pdfplumber PowerDB grid parser
# + Tesseract OCR now RUN in prod. Prior to c652578 the image was node:20-alpine
# and pypdfium2 had no musl wheel, so the deterministic extractor was inert and
# ingest silently fell back to the pdfjs parser + AI gap-fill. If you ever
# switch the runtime image back to Alpine, this comment (and the extractor
# gating) must both be revisited — see docs/PDF_INGESTION_SYNTHESIS_2026-07-03.md
# §2 and docs/PROD_REALITY_AUDIT_2026-07-04.md for the incident history.
